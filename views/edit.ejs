<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CRUD APP</title>
    <link rel="stylesheet" href="/style/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
      <div class="nav mb-10">
        <a class="text-blue-600" href="/read">Read Users</a>
      </div>
      <h1 class="text-xl font-semibold mb-4 text-center">Edit User</h1>
      <form
        action="/update/<%= user._id %>"
        method="POST"
        class="flex flex-col gap-3 max-w-md mx-auto bg-zinc-800 p-6 rounded-lg"
      >
        <input
          class="px-5 py-2 rounded text-black"
          placeholder="Name"
          name="name"
          value="<%= user.name %>"
        />
        <input
          class="px-5 py-2 rounded text-black"
          placeholder="Email"
          name="email"
          value="<%= user.email %>"
        />
        <input
          class="px-5 py-2 rounded text-black"
          placeholder="Gender"
          name="gender"
          value="<%= user.gender %>"
        />
        <input
          class="bg-blue-600 text-white px-5 py-2 rounded hover:bg-blue-700 cursor-pointer"
          type="submit"
          value="Update User"
        />
      </form>
    </div>
  </body>
</html>
