<!DOCTYPE html>
<html lang="en">
<head> … </head>
<body>
  <a href="/">← Return to Home</a>
  <h1>All Teachers</h1>
  <% if (allTeachers && allTeachers.length) { %>
  <% allTeachers.forEach(t => { %>
    <h2><%= t.name %></h2>
  <% }) %>
<% } else { %>
  <p>No teachers found.</p>
<% } %>

</body>
</html>
